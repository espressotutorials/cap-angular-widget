name: CI

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        id: install
        run: npm ci --include=dev 2>&1 | tee npm-ci.log
      - name: Report install failure
        if: failure() && steps.install.outcome == 'failure'
        run: |
          while IFS= read -r line; do
            echo "::error file=package-lock.json::$line"
          done < <(tail -n 30 npm-ci.log)
      - name: Test library
        id: test-library
        run: npm test 2>&1 | tee npm-test.log
      - name: Report library test failure
        if: failure() && steps.test-library.outcome == 'failure'
        run: |
          while IFS= read -r line; do
            echo "::error file=projects/cap-angular-widget/src/lib/capjs-widget.component.spec.ts::$line"
          done < <(tail -n 40 npm-test.log)
      - name: Test demo
        id: test_demo
        run: npm run test:demo 2>&1 | tee npm-test-demo.log
      - name: Report demo test failure
        if: failure() && steps.test_demo.outcome == 'failure'
        run: |
          while IFS= read -r line; do
            echo "::error file=src/app/app.component.spec.ts::$line"
          done < <(tail -n 40 npm-test-demo.log)
      - name: Build library and demo
        id: build
        run: npm run build 2>&1 | tee npm-build.log
      - name: Report build failure
        if: failure() && steps.build.outcome == 'failure'
        run: |
          while IFS= read -r line; do
            echo "::error file=package.json::$line"
          done < <(tail -n 40 npm-build.log)
